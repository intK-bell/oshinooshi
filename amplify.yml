version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm --prefix ./web ci
    build:
      commands:
        - npm --prefix ./web run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**
  appRoot: web
