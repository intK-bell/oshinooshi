version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm --prefix ./web ci
    build:
      commands:
        - npm --prefix ./web run build
  artifacts:
    baseDirectory: web/.next
    files:
      - '**/*'
  cache:
    paths:
      - web/node_modules/**
  appRoot: web